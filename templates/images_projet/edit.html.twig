{% extends 'base.html.twig' %}

{% block title %}Edit ImagesProjet{% endblock %}

{% block body %}
    <section id="projetn4">
        <h1 class="title1" id="titre4">modifier ImagesProjet</h1>
        {% for message in app.flashes('imageError') %}
            <div class="flash-success">
                {{ message }}
            </div>
        {% endfor %}
        {{ include('images_projet/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_images_projet_index') }}" id="link2" class="link">back to list</a>

        {{ include('images_projet/_delete_form.html.twig') }}
    </section>
{% endblock %}
